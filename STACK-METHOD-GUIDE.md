# スタック方式の選び方

この資料は、`--stack-method`を指定するときの目安です。方式によって、星・人工衛星・宇宙線などの一時的な明るい成分の除去、彗星信号の扱い、処理時間が変わります。

## まず選ぶなら

- **通常の彗星・小惑星スタック:** `mean`
- **星トラックや人工衛星をできるだけ減らしたい:** `mad-clip`
- **外れ値除去を軽く試したい:** `sigma-clip`
- **最も強い外れ値除去を試したい:** `winsorized-sigma`
- **単純で分かりやすい外れ値除去:** `median`

既定値はこれまでどおり`mean`です。良好なフレームだけを使う場合、平均は一般に最も高いS/Nを得やすく、彗星の光度測定にも扱いやすい方式です。

## 方式の違い

### `mean`

各画素の有効な値を算術平均します。ノイズを最も効率よく減らせますが、少数フレームにだけ現れる人工衛星、飛行機、宇宙線、ホットピクセルなどの明るい成分が残りやすくなります。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" --stack-method mean
```

### `median`

各画素の中央値を使います。少数フレームの外れ値に強く、星トラックを大きく減らせます。一方、平均より遅く、同じ画素に外れ値が長く存在すると完全には消えません。通常は平均よりS/Nが低くなるため、光度測定では結果を`mean`または`mad-clip`と比較してください。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" --stack-method median
```

### `sigma-clip`

各画素について通常の平均と標本標準偏差を計算し、指定した範囲外を除いて残りを平均します。`median`より平均に近い結果を得やすく、`winsorized-sigma`より手軽です。ただし、非常に明るい外れ値が平均と標準偏差を押し上げると、除去が弱くなることがあります。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" --stack-method sigma-clip
```

### `mad-clip`

中央値と`1.4826 x MAD`（Median Absolute Deviation）で外れ値を判定し、残った元の値を平均します。平均と標準偏差を使う方式より外れ値の影響を受けにくく、今回の測定では平均に近い彗星S/Nを保ちながら星トラックを大きく減らしました。外れ値除去と処理時間のバランスを取りたい場合の有力な選択です。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" --stack-method mad-clip
```

### `winsorized-sigma`

SirilのWinsorized Sigma Clippingに合わせ、外れ値の影響を抑えたsigmaを内部反復で推定した後、元のサンプルを除外して平均します。今回の測定では星トラックの残留が最も少なくなりましたが、5方式の中で最も時間がかかりました。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" --stack-method winsorized-sigma
```

## 閾値の指定

`sigma-clip`、`mad-clip`、`winsorized-sigma`は共通して、暗い側と明るい側の閾値を`sigma`倍率で指定できます。既定値は上下とも3です。

```powershell
.\seestar-metcalf-stack.cmd "C:\path\to\frames" `
  --stack-method mad-clip --clip-low 3 --clip-high 3
```

例えば`--clip-low 5 --clip-high 2`なら、暗い側は`-5 sigma`、明るい側は`+2 sigma`を超えた値だけを除外します。天体そのものの一時的な変化まで除外する可能性があるため、測光では強すぎる閾値を避け、方式を変えた結果も確認してください。

## 実測による比較

220P/McNaughtの同じセッション2、247枚を同じ位置合わせ・同じ二次背景補正で処理した結果です。処理時間は一台のWindows環境での実測であり、CPU、メモリ、ディスクキャッシュなどで変わります。

| 方式 | スタック時間 | 全工程 | 星トラック残留の減少* | 背景σ | 彗星開口S/N** |
|---|---:|---:|---:|---:|---:|
| `mean` | 100秒 | 149秒 | 0% | 8.785 ADU | 183.69 |
| `median` | 236秒 | 286秒 | 85.9% | 9.813 ADU | 166.44 |
| `mad-clip` | 288秒 | 349秒 | 86.8% | 8.793 ADU | 183.97 |
| `sigma-clip` | 369秒 | 432秒 | 62.0% | 8.802 ADU | 183.33 |
| `winsorized-sigma` | 577秒 | 631秒 | 90.5% | 8.784 ADU | 184.30 |

\* 48個の星、半径8ピクセルの予測軌跡で測った高周波残差を`mean`と比較した値です。星の個数を直接数えた値ではありません。`mean`の残留を100%としたとき、`winsorized-sigma`は残留を約9.5%まで減らしました。

\*\* 彗星中心の半径8ピクセル開口と、18--32ピクセルの背景環から求めた相対的なS/Nです。絶対測光値ではありません。

## 使い分けの目安

1. **速度とS/Nを優先:** `mean`
2. **星トラックを減らしつつ彗星信号を保ちたい:** `mad-clip`
3. **まず軽く外れ値除去を試す:** `sigma-clip`
4. **処理時間より星トラック除去を優先:** `winsorized-sigma`
5. **中央値の結果を確認したい、または単純な方式を使いたい:** `median`

この実測では、`mad-clip`が`mean`に近いS/N、`median`と同程度の星トラック除去、`winsorized-sigma`より短い処理時間を両立しました。ただし撮影条件や星の明るさ、雲、人工衛星の通過状況で結果は変わるため、重要な測定では`mean`と`mad-clip`を比較することをおすすめします。

クリップ方式は、移動天体として位置合わせされた彗星を通常は各フレームの同じ画素に集めるため、星トラックだけを主に外れ値として扱えます。しかし、雲、欠損、飽和、彗星の広い尾や急な変化がある場合は、信号の一部が除外される可能性があります。飽和の有無は`--saturation-warning enable`も併用して確認してください。
